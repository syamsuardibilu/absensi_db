# === Mode ===
# development untuk lokal; produksi di server/Render isi via dashboard (optional)
NODE_ENV=development

# === Server Bind ===
HOST=0.0.0.0
PORT=3000

# === URL / Deployment ===
# Urutan prioritas (server.js akan mengurus):
# 1. RENDER_EXTERNAL_URL (otomatis di Render.com)
# 2. BASE_URL (jika kamu isi manual di hosting lain)
# 3. http://127.0.0.1:PORT (fallback dev)
# NOTE: Biasanya kamu TIDAK perlu mengisi BASE_URL saat dev.
# BASE_URL=https://absensi-db.onrender.com

# === Database ===
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=absensi_db

# === CORS (tambahkan domain produksi kamu di sini bila perlu) ===
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:5500,https://daengbilu.com,https://www.daengbilu.com
